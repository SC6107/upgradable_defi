# API Configuration
# IMPORTANT: Use /api for local development to avoid CORS issues
# The Vite proxy (configured in vite.config.ts) will forward /api requests to http://localhost:8000
VITE_API_URL=/api

# Optional: Set to production API when deploying
# VITE_API_URL=https://api.yourdomain.com

# DO NOT use http://localhost:8000 directly - it will cause CORS errors!
